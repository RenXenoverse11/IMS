{
  "name": "internship-management-system",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "build:gas": "npm run build && node scripts/prepare-gas.mjs",
    "gas:login": "npx clasp -P appscript -I appscript login",
    "gas:status": "npx clasp -P appscript -I appscript status",
    "gas:push": "npm run build:gas && npx clasp -P appscript -I appscript push",
    "gas:deploy": "npm run build:gas && npx clasp -P appscript -I appscript push && npx clasp -P appscript -I appscript deploy",
    "gas:open": "npx clasp -P appscript -I appscript open",
    "push:gas": "npm run gas:push",
    "deploy:gas": "npm run gas:deploy"
  },
  "devDependencies": {
    "@google/clasp": "^3.0.6",
    "@sveltejs/vite-plugin-svelte": "^7.0.0",
    "autoprefixer": "^10.4.20",
    "svelte": "^5.53.12",
    "tailwindcss": "^3.4.9",
    "vite": "^8.0.1"
  },
  "dependencies": {
    "lucide-svelte": "^1.0.1"
  }
}
